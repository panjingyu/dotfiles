- defaults:
    link:
        create: true
        relink: true

- clean:
    ~/:
    ~/.config:
        recursive: true

- link:
    ~/.config/git: git
    ~/.tmux.conf: tmux/tmux.conf
    ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]

